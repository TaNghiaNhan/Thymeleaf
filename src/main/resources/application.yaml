server:
  port: 8080
  servlet:
    context-path: /CRUD

spring:
  datasource:
    url: "jdbc:mysql://localhost:3306/demoThymeleaf"
    username: root
    password: 1234
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  thymeleaf:
    cache: false